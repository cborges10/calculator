spring.application.name=calculator

# Use a non-default port to avoid conflicts during development
server.port=8081
# Bind to all network interfaces so the app is accessible from outside the container
server.address=0.0.0.0
